/**
 * User-visible release marker. Keep this in sync with app.config.ts and
 * public/manifest.json whenever a published PWA build changes.
 */
export const RELEASE_VERSION = 'v1.1.1';
export const RELEASE_LABEL = `BUILD ${RELEASE_VERSION}`;
